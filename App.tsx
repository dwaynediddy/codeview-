import {
  SafeAreaView,
  ScrollView,
  StyleSheet,
  View,
} from 'react-native';

const App = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <View>

        </View>
      </ScrollView>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({

});

export default App;
